建立一个git库：
	第一步，创建一个文件夹，进入；
	第二步，用“git init”命令将这个目录变成Git可以管理的仓库，此时目录下多了个.get的目录，如果看不见就是隐藏了，利用命令“ls -ah”

讲一个文件放到仓库：
	第一步，“git add 文件名”，这一步可以添加多次；
	第二步，“get commit -m 文件改动描述”；

git的增删回退：
	首先“git status”查看结果，发现说是有修改，但没有提交；
	然后我们想知道修改了什么，怎么修改的“git diff”，可以看到修改了什么